version: v1
kind: task_routing
default_profile: balanced
routes:
  understanding.extract:
    profile: balanced
    fallback_profiles: [fast]
  summarization.chunk:
    profile: fast
    fallback_profiles: [balanced]
  summarization.consolidate:
    profile: balanced
    fallback_profiles: [quality]
  clarification.generate:
    profile: balanced
  outline.generate:
    profile: quality
    fallback_profiles: [balanced]
  deck_spec.generate:
    profile: quality
    fallback_profiles: [balanced]
  qa.evaluate:
    profile: balanced
